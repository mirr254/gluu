# Default values for gluu.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  # configMap values that are mostly used
  gluuConfigAdapter: "kubernetes"
  gluuSecretAdapter: "kubernetes"