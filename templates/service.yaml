apiVersion: v1
kind: Service
metadata:
  name: {{ include "gluu.fullname" . }}
  labels:
    app: {{ include "gluu.name" . }}
    chart: {{ include "gluu.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "gluu.name" . }}
    release: {{ .Release.Name }}
